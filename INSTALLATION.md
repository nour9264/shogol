# دليل التثبيت والتشغيل - منصة شغل

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- **Node.js** (الإصدار 18 أو أحدث) - [تحميل](https://nodejs.org/)
- **npm** (يأتي مع Node.js) أو **yarn**
- محرر نصوص (يُفضل VS Code)

## خطوات التثبيت

### 1. التحقق من تثبيت Node.js

```bash
node --version
npm --version
```

يجب أن تظهر أرقام الإصدارات.

### 2. تثبيت المكتبات

افتح Terminal/Command Prompt في مجلد المشروع وقم بتشغيل:

```bash
npm install
```

أو إذا كنت تستخدم yarn:

```bash
yarn install
```

### 3. إعداد ملف البيئة

الملف `.env` موجود بالفعل مع الإعدادات التالية:

```env
VITE_API_BASE_URL=https://localhost:7035/api
VITE_WEBSOCKET_URL=https://localhost:7035/chatHub
VITE_APP_NAME=شغل
```

**ملاحظة**: تأكد من أن الـ Backend API يعمل على `https://localhost:7035`

### 4. تشغيل المشروع

#### وضع التطوير (Development)

```bash
npm run dev
```

سيفتح المتصفح تلقائياً على `http://localhost:3000`

#### بناء النسخة الإنتاجية (Production Build)

```bash
npm run build
```

سيتم إنشاء مجلد `dist` يحتوي على الملفات الجاهزة للنشر.

#### معاينة النسخة الإنتاجية

```bash
npm run preview
```

## حل المشاكل الشائعة

### مشكلة: `npm install` يفشل

**الحل**:
```bash
# احذف المجلدات القديمة
rm -rf node_modules package-lock.json

# ثبت من جديد
npm install
```

### مشكلة: خطأ في الاتصال بالـ API

**الحل**:
1. تأكد من تشغيل الـ Backend API
2. تحقق من عنوان API في ملف `.env`
3. إذا كان البروتوكول `https`، قد تحتاج لقبول الشهادة في المتصفح

### مشكلة: الخطوط العربية لا تظهر

**الحل**: تأكد من اتصالك بالإنترنت لتحميل خطوط Google Fonts

### مشكلة: الصفحة فارغة

**الحل**:
1. افتح Console في المتصفح (F12)
2. ابحث عن أي أخطاء
3. تأكد من أن الـ API URL صحيح

## هيكل المشروع

```
ShogolFront/
├── public/              # الملفات الثابتة
├── src/
│   ├── components/      # المكونات القابلة لإعادة الاستخدام
│   │   ├── Common/      # مكونات عامة (Loading, Modal, Toast...)
│   │   ├── Cards/       # بطاقات العرض
│   │   └── Layout/      # Header, Footer
│   ├── pages/           # صفحات التطبيق
│   │   ├── Auth/        # صفحات التسجيل والدخول
│   │   ├── Jobs/        # صفحات المشاريع
│   │   ├── Freelancers/ # صفحات المستقلين
│   │   └── Profile/     # صفحات الملف الشخصي
│   ├── context/         # Context API (AuthContext)
│   ├── services/        # API Services
│   ├── hooks/           # Custom Hooks
│   ├── utils/           # دوال مساعدة
│   ├── App.jsx          # المكون الرئيسي
│   ├── main.jsx         # نقطة البداية
│   └── index.css        # التنسيقات العامة
├── package.json
├── vite.config.js
└── tailwind.config.js
```

## المميزات المنفذة

### ✅ نظام المصادقة (Authentication)
- تسجيل مستخدم جديد
- تسجيل الدخول
- التحقق من OTP
- إدارة JWT Token

### ✅ إدارة المشاريع (Jobs)
- عرض قائمة المشاريع
- تفاصيل المشروع
- نشر مشروع جديد
- البحث والتصفية

### ✅ المستقلون (Freelancers)
- عرض قائمة المستقلين
- صفحة الملف الشخصي
- البحث والتصفية

### ✅ العروض (Proposals)
- إرسال عرض على مشروع
- قبول/رفض العروض

### ✅ الملف الشخصي (Profile)
- عرض الملف الشخصي
- تعديل المعلومات الشخصية

### ✅ واجهة المستخدم
- تصميم عصري ومتجاوب
- دعم كامل للغة العربية (RTL)
- متوافق مع جميع الأجهزة
- رسائل نجاح/خطأ (Toasts)

## الصفحات المتوفرة

| المسار | الوصف | حماية |
|--------|-------|-------|
| `/` | الصفحة الرئيسية | عامة |
| `/login` | تسجيل الدخول | عامة |
| `/register` | التسجيل | عامة |
| `/verify-otp` | التحقق من OTP | عامة |
| `/jobs` | قائمة المشاريع | عامة |
| `/jobs/:id` | تفاصيل المشروع | عامة |
| `/post-job` | نشر مشروع | للعملاء فقط |
| `/freelancers` | قائمة المستقلين | عامة |
| `/freelancers/:id` | ملف المستقل | عامة |
| `/profile` | الملف الشخصي | محمية |
| `/profile/edit` | تعديل الملف | محمية |
| `/messages` | الرسائل | محمية |
| `/notifications` | الإشعارات | محمية |
| `/my-jobs` | مشاريعي | محمية |

## API Endpoints المستخدمة

جميع الـ Endpoints موثقة في ملف `API_DOCUMENTATION.md`

### الأساسية:
- `POST /api/Auth/register` - تسجيل
- `POST /api/Auth/login` - دخول
- `POST /api/Auth/verify-otp` - تحقق OTP
- `GET /api/User/profile` - الملف الشخصي
- `POST /api/JobRequest` - نشر مشروع
- `GET /api/JobRequest/search` - بحث مشاريع
- `POST /api/Proposal` - إرسال عرض

## التطوير المستقبلي

### المخطط له:
- [ ] نظام المحادثات الفورية (WebSocket)
- [ ] نظام الإشعارات الحية
- [ ] رفع الملفات والصور
- [ ] نظام التقييمات والمراجعات
- [ ] نظام الدفع الإلكتروني
- [ ] لوحة تحكم إدارية
- [ ] إحصائيات وتقارير
- [ ] نظام البحث المتقدم
- [ ] تطبيق الموبايل

## الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تحقق من Console المتصفح (F12)
2. تأكد من تشغيل Backend API
3. راجع ملف `API_DOCUMENTATION.md`
4. تحقق من ملف `.env`

## الترخيص

جميع الحقوق محفوظة © 2024 شغل - منصة العمل الحر الاحترافية

